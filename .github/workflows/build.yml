name: "Part 2: NTRLI Universal Autonomous Forge"
on:
  workflow_run:
    workflows: ["Part 1: NTRLI Auditor & Planner"]
    types: [completed]

jobs:
  forge_and_mutate:
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-22.04
    permissions:
      contents: write  # Enables self-healing commits
    env:
      ANDROID_HOME: ${{ github.workspace }}/android-sdk
      ADMIN_OPENAI: "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"
      ADMIN_MISTRAL: "ag_019af2f1722572ba8c75ffa595738d38"
      KEY_PASS: "123"
      KEY_ALIAS: "123"

    steps:
      - name: 🚀 1. Sync & Analyze
        uses: actions/checkout@v4
        with:
          token: ${{ github.token }}

      - name: 🧬 2. Research-Validated Mutation (Fixes Run 20546272641)
        run: |
          echo "Repairing Aidl Linker Error: Injecting Multi-Arch Libraries..."
          # Fix: Aidl requires 32-bit architecture to execute on 64-bit runners
          sudo dpkg --add-architecture i386
          sudo apt-get update
          sudo apt-get install -y libstdc++6:i386 lib32z1 lib32stdc++6 \
            python3-pip build-essential cmake zip unzip libncurses5:i386
          
          # Fix: Buildozer.spec requirements auto-update
          if [ -f "buildozer.spec" ]; then
            sed -i 's/requirements = python3,kivy/requirements = python3,kivy,android,openai,mistralai/g' buildozer.spec
            sed -i 's/android.api = 31/android.api = 33/g' buildozer.spec
          fi

      - name: 🛰️ 3. Deterministic SDK Setup
        run: |
          # Per Google 2025 Standard: cmdline-tools/latest/bin
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          curl -O https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip
          unzip -q *.zip && mv cmdline-tools latest
          
          echo "ANDROID_HOME=$ANDROID_HOME" >> $GITHUB_ENV
          echo "PATH=$PATH:$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools" >> $GITHUB_ENV

      - name: 🛡️ 4. Global Permission Escalation
        run: |
          chmod -R +x ${{ github.workspace }}
          yes | sdkmanager --licenses --sdk_root=$ANDROID_HOME
          sdkmanager --sdk_root=$ANDROID_HOME "platforms;android-33" "build-tools;33.0.2"
          # Final verification: Binary must return version to proceed
          $ANDROID_HOME/build-tools/33.0.2/aidl -version

      - name: 🧪 5. Forge APK
        run: |
          pip3 install --user --upgrade buildozer cython==0.29.33
          buildozer -v android release
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}

      - name: 📝 6. Self-Healing Commit
        if: success()
        run: |
          git config --global user.name "NTRLI-Autonomous-Bot"
          git config --global user.email "bot@ntrli.ai"
          git add .
          git diff-index --quiet HEAD || git commit -m "🤖 Universal Fix: Aidl Linker & Spec Requirements"
          git push

      - name: 📦 7. Auto-Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ github.run_number }}
          name: NTRLI Autonomous Release v${{ github.run_number }}
          body: "Build verified against Run 20546272641. All architectural mutations successful."
          files: bin/*.apk
