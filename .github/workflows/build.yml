name: NTRLI Absolute God-Mode Forge
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  forge_and_mutate:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    env:
      ANDROID_HOME: ${{ github.workspace }}/android-sdk
      ANDROID_NDK_HOME: ${{ github.workspace }}/android-ndk
      BUILDOZER_BIN: /home/runner/.local/bin/buildozer
      # Resonance Protocol: Forcing global path recognition
      BUILDOZER_SDK_PATH: ${{ github.workspace }}/android-sdk
      BUILDOZER_NDK_PATH: ${{ github.workspace }}/android-ndk
      CI: true

    steps:
      - name: 🚀 1. Atomic Sync & Branch Sovereignty
        uses: actions/checkout@v4

      - name: ☕ 2. Java 17 Enforcement (Zulu Protocol)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🧬 3. Multi-AI Dependency Injection
        run: |
          sudo dpkg --add-architecture i386
          sudo apt-get update -qq
          sudo apt-get install -y -qq build-essential ccache cmake curl git \
            libffi-dev libssl-dev libstdc++6:i386 lib32z1 lib32stdc++6 \
            libncurses5:i386 openjdk-17-jdk python3-dev python3-pip unzip zip \
            zlib1g-dev autoconf automake libtool pkg-config lld
          pip3 install --user --upgrade buildozer cython==0.29.33 flake8
          echo "/home/runner/.local/bin" >> $GITHUB_PATH

      - name: 🛰️ 4. SDK/NDK Atomic Construction (Absolute Pivot)
        run: |
          # Clean slate to prevent legacy path contamination
          rm -rf $ANDROID_HOME
          mkdir -p $ANDROID_HOME/cmdline-tools/latest
          
          # Atomic Download & Staging
          curl -sS -L https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -o cmdline.zip
          unzip -q cmdline.zip -d temp_sdk
          # Fix: Strategic move to satisfy Google's 'latest/bin' requirement
          cp -r temp_sdk/cmdline-tools/* $ANDROID_HOME/cmdline-tools/latest/
          
          curl -sS -L https://dl.google.com/android/repository/android-ndk-r25b-linux.zip -o ndk.zip
          unzip -q ndk.zip
          mv android-ndk-r25b $ANDROID_NDK_HOME

      - name: 🛡️ 5. Superior Integrity Audit (Resilience Lockdown)
        run: |
          SDK_MGR=$ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager
          chmod +x $SDK_MGR
          
          # Force accept licenses via Absolute Path
          yes | $SDK_MGR --sdk_root=$ANDROID_HOME --licenses
          
          # Resilience: Pre-install core tools so Buildozer doesn't have to 'think'
          $SDK_MGR --sdk_root=$ANDROID_HOME "platforms;android-33" "build-tools;33.0.2" "platform-tools"
          
          # Total Sovereignty: Unlock all directories for runner user
          sudo chmod -R 777 $GITHUB_WORKSPACE
          
          # Terminal Verification
          $ANDROID_HOME/build-tools/33.0.2/aidl -version || exit 1

      - name: 🧪 6. Path-Locked Execution (Zero-Guessing Mode)
        run: |
          # Mutate spec live to align with Absolute Paths
          if [ ! -f "buildozer.spec" ]; then $BUILDOZER_BIN init; fi
          sed -i "s|^#\?android.sdk_path = .*|android.sdk_path = $ANDROID_HOME|g" buildozer.spec
          sed -i "s|^#\?android.ndk_path = .*|android.ndk_path = $ANDROID_NDK_HOME|g" buildozer.spec
          sed -i "s|^#\?android.accept_sdk_license = .*|android.accept_sdk_license = True|g" buildozer.spec
          # Resilience: Inject all necessary AI & SSL libs directly into the binary blueprint
          sed -i "s|^requirements = .*|requirements = python3,kivy,android,openai,mistralai,urllib3==1.26.15,certifi,charset-normalizer,openssl,requests|g" buildozer.spec
          
          # Path Shielding: Placing our tools at the top of the OS hierarchy
          export PATH=$ANDROID_HOME/platform-tools:$ANDROID_HOME/build-tools/33.0.2:/home/runner/.local/bin:$PATH
          $BUILDOZER_BIN -v android release 2>&1 | tee build_logs.txt
        env:
          ANDROID_HOME: ${{ env.ANDROID_HOME }}
          ANDROID_NDK_HOME: ${{ env.ANDROID_NDK_HOME }}

      - name: 📊 7. Final Resilience Report
        if: always()
        run: |
          echo "### 📱 NTRLI Absolute God-Mode Status" >> $GITHUB_STEP_SUMMARY
          if [ -d "bin" ] && [ "$(ls -A bin/*.apk 2>/dev/null)" ]; then
            echo "✅ **RESILIENCE ACHIEVED: APK FORGED**" >> $GITHUB_STEP_SUMMARY
            echo "- File: $(ls bin/*.apk)" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **BUILD RESISTANCE DETECTED**" >> $GITHUB_STEP_SUMMARY
            echo '```text' >> $GITHUB_STEP_SUMMARY
            [ -f build_logs.txt ] && tail -n 50 build_logs.txt >> $GITHUB_STEP_SUMMARY || echo "No logs generated." >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi

      - name: 📦 8. Instant Mobile Release
        if: success()
        uses: softprops/action-gh-release@v2
        with:
          tag_name: release-${{ github.run_id }}
          files: bin/*.apk
          token: ${{ github.token }}
